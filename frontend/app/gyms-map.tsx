import React from 'react';
// The bundler will automatically pick .web.tsx or .native.tsx based on the platform!
import GymsMapContent from '../components/GymsMapContent';

export default function GymsMapScreen() {
  return <GymsMapContent />;
}
